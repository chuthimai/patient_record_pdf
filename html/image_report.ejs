<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <title>Phiếu <%= typeOfService%></title>
    <style>
        body {
            font-family: "Times New Roman", serif;
            font-size: 14px;
            margin: 40px;
            line-height: 1.5;
        }
        p {
            margin: 0;
        }
        h2 {
            text-align: center;
            text-transform: uppercase;
            font-size: 18px;
            margin-bottom: 5px;
        }

        div {
            margin-bottom: 8px;
            margin-top: 8px;
        }

        div > div {
            margin-top: 1px;
            margin-bottom: 1px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
        }
        .header .left {
            width: 60%;
        }
        .header .right {
            text-align: right;
            width: 40%;
        }


        .section {
            margin-top: 10px;
        }
        .box {
            border: 1px solid #000;
            padding: 8px;
            min-height: 100px;
            margin: 10px 0;
        }
        /* base: grid, gap */
        .images {
            display: grid;
            gap: 12px;
            align-items: start;
        }

        /* 1 ảnh -> full width */
        .images:has(img:only-child) {
            grid-template-columns: repeat(2, 1fr);
        }

        /* 2 ảnh -> 2 columns */
        .images:has(img:nth-child(2)):not(:has(img:nth-child(3))) {
            grid-template-columns: repeat(2, 1fr);
        }

        /* 3 ảnh -> 3 columns */
        .images:has(img:nth-child(3)):not(:has(img:nth-child(4))) {
            grid-template-columns: repeat(3, 1fr);
        }

        /* >=4 ảnh -> 4 columns */
        .images:has(img:nth-child(4)) {
            grid-template-columns: repeat(4, 1fr);
        }

        /* ảnh luôn lấp đầy ô */
        .images img {
            width: 100%;
            height: auto;
            display: block;
            object-fit: cover;
            border-radius: 6px;
            border: 1px solid #ccc;
        }

        .signature-row {
            display:flex;
            justify-content:space-between;
            align-items:flex-start;
            margin-top: 20px;
            font-size:13px;
        }
        .sig-right {
            text-align:center;
            width:40%;
        }
        .signature-line {
            margin-top:36px;
            width: 80%;
            margin-left: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }
        td {
            vertical-align: top;
            padding: 3px 0;
        }
        .footer {
            margin-top: 20px;
            display: flex;
            justify-content: space-between;
        }
    </style>
</head>
<body>
<div class="header">
    <div class="left">
        <p>Sở Y tế: <%= departmentOfHealth%></p>
        <p>BV: <%= hospital%></p>
    </div>
    <div class="right">
        <p>Mã số: MS:08/BV-02</p>
        <p>Số: <%= recordNumber%></p>
    </div>
</div>

<!--serviceName-->
<h2>PHIẾU <%= typeOfService.toUpperCase()%></h2>

<div>
    <table>
        <tr>
            <td>- Họ tên người bệnh: <strong><%= patientName %></strong></td>
            <td>Ngày sinh: <%= birthDate %></td>
            <td>Nam/Nữ: <%= gender %></td>
        </tr>
        <tr>
            <td colspan="3">- Địa chỉ: <%= addressOfPatient %></td>
        </tr>
        <tr>
            <td>- Khoa: <%= specialize %></td>
            <td>Phòng: <%= address %></td>
        </tr>
        <tr>
            <td colspan="3">- Chẩn đoán: <%= conclusionOfClinicalDiagnosis %></td>
        </tr>
    </table>
</div>

<div class="section">
    <strong>Yêu cầu chiếu/chụp</strong>
    <div class="box">
        <%= request %>
    </div>
</div>

<div class="signature-row">
    <div></div>
    <div class="sig-right">
        <p><%= requestDate%></p>
        <p><strong>Bác sĩ điều trị</strong></p>
        <div class="signature-line"></div>
        <p><%= treatingDoctor%></p>
    </div>
</div>

<div class="section">
    <strong>Kết quả chiếu/chụp</strong>
    <div class="images {{images.length > 4 ? 'many' : 'few'}}">
<!--        media-->
        <% for (var i = 0; i < media.length; i++) { %>
        <img src="<%= media[i] %>" alt=""/>
        <% }%>
    </div>

    <div class="box">
<!--        assessmentItems -->
        <%- detailContent%>
<!--        interpretation-->
        <p>Diễn giải, giải thích ảnh: <%= interpretation%></p>
    </div>
</div>

<div class="signature-row">
    <div class="small-note">
        Lời dặn của BS chuyên khoa:<br/>
        <em>{{specialistNote}}</em>
    </div>
    <div class="sig-right">
        <p><%= resultDate%></p>
        <p><strong>Bác sĩ chuyên khoa</strong></p>
        <div class="signature-line"></div>
        <p><%= specialistDoctor%></p>
    </div>
</div>

</body>
</html>
